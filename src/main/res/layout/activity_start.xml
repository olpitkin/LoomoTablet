<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:labelFor="@string/app_name"
    android:orientation="vertical" >

    <Button
        android:id="@+id/start"
        android:layout_width="300dp"
        android:layout_height="70dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:onClick="startClicked"
        android:text="@string/start"
        android:textSize="15sp" />

    <ToggleButton
        android:id="@+id/load_adf"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:onClick="loadAdfClicked"
        android:textOff="@string/load_adf_off"
        android:textOn="@string/load_adf_on"
        android:textSize="15sp" />

    <Button
        android:id="@+id/AdfListView"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="@string/manage_adfs"
        android:textSize="15sp"
        android:onClick="adfListViewClicked"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true" />

    <Button
        android:id="@+id/PoiListView"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="@string/manage_pois"
        android:textSize="15sp"
        android:onClick="poiListViewClicked"
        android:layout_above="@+id/AdfListView"
        android:layout_alignParentEnd="true" />

    <Button
        android:id="@+id/PathListView"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="Manage Path"
        android:textSize="15sp"
        android:onClick="pathListViewClicked"
        android:layout_above="@+id/PoiListView"
        android:layout_alignParentEnd="true" />

    <Button
        android:id="@+id/InfoListView"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="Manage Info"
        android:textSize="15sp"
        android:onClick="infoListViewClicked"
        android:layout_above="@+id/PathListView"
        android:layout_alignParentEnd="true" />


</RelativeLayout>
